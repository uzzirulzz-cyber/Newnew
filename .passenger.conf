# Passenger app configuration for cPanel Node.js deployment.
# This file is used by Phusion Passenger (cPanel's Node.js app runner).

AppType node
StartupFile server.js
AppName "PlayBeat Digital"
AppEnv production

# Set environment variables here (alternatively use cPanel UI)
SetEnv NODE_ENV=production
